# 个人作品集讲解脚本

> 每个项目先讲整体设计，再拆能力点；每段能力标注我匹配的图。

---

## 治擎 —— 整车成本财务 AI 系统

**整体设计**
把整车成本全生命周期（BOM → 采购 → VAVE → 核算 → 对标 → 归档）从分散的 Excel 整合成一个统一工作台，右下角一个 AI 助手贯穿始终。核心思路是：AI 做导航，人做决策。

1. **AI 对话导航** → 图 `cost-01-dashboard.png`
   AI 助手理解业务上下文（当前车型、当前页），一句话下钻数据、跳转页面。定位是导航型 Agent，只带路、不拍板。

2. **BOM 三级下钻** → 图 `cost-02-bom.png`
   整车 → 系统 → 单件，每级对比目标成本 vs 应该成本，零件超支能追溯到具体工艺，把成本颗粒度做到工艺级。

3. **竞品对标** → 图 `cost-05-benchmark.png`
   横向拆解对标车型的成本结构与市场定价，输出差距在哪、差多少。

4. **数据溯源** → 图 `cost-06-archive.png`
   成本域数据资产统一管理，任何数字可追问「从哪来、谁改的、为什么」，支撑审计闭环。

---

## 星核 3.0 —— 企业级 AI Agent 平台

**整体设计**
一个平台覆盖四大行业，每个行业有独立的工作项与 Agent；进去后是统一的企业级设计，核心解决「AI 进企业，谁负责、怎么审计」。

**先看四大行业** → 图 `star-00-main.png`

电商（大促 ROI / 库存 / 差评）、医药（偏差 / CAPA / 批放行）、教育（续费 / 报名 / 投诉）、金融（授信 / 风控 / 合规）。四个行业共享一套骨架，数据与 Agent 完全隔离，新增行业 = 配数据 + 配工作项，不重写平台。

**再看企业级设计**

1. **Agent 编排落地** → 图 `ind-2-pharma-workspace.png`
   岗位工作台的工作项不是人手工录入，而是 Agent 先生成专业分析（调查报告、根因、结论），人负责裁决与推进。

2. **人在回路（HUMAN_ONLY）** → 图 `ind-2-pharma-detail.png` ★
   Agent 输出 → 人判断 → 继续执行；放行 / 授信等终判在人，AI 只给建议。

3. **全程可审计** → 图 `ind-2-pharma-governance.png` ★
   审计日志是真哈希链，每步判定留痕；协作链「认 / 改 / 打回」回流成 Agent 信用账本。

---

## PromptLab —— Prompt 工程产品（已上线）

**整体设计**
把 Prompt 工程从「手工笔记」做成产品，覆盖资产管理、编辑、实验验证三步闭环。核心思路：让调 Prompt 可管理、可复现、可量化。

**能力点**

1. **产品定位** → 图 `promptlab-01-hero.png`
   面向 Prompt 工程的工具，已上线 v1.0，有真实使用闭环。

2. **Prompt 资产管理** → 图 `promptlab-02-dashboard.png`
   Prompt 资产、版本、使用情况统一管理，可复现。

3. **编辑闭环** → 图 `promptlab-03-editor.png`
   写 Prompt、跑模型、看输出在一个界面完成，迭代不切上下文。

4. **量化实验** → 图 `promptlab-04-experiment.png`
   A/B 对比不同 Prompt 的效果，用数据替代主观感觉做迭代决策。

---

## 收尾

三个项目一条主线：AI 进工业（治擎）、AI 进组织（星核）、AI 成产品（PromptLab）。贯穿始终的三个能力：**可溯源、可审计、可量化**。

## 附：高频追问

- **治擎和星核是不是一套代码？** → 不是。治擎单行业做深，星核多行业做平台。
- **星核 Agent 真跑模型还是写死数据？** → 架构是真 Agent 编排（协作链、信用账本、哈希审计），工作项是演示数据，重点是「人在回路 + 可审计」机制。
- **HUMAN_ONLY 什么意思？** → 合规红线。AI 只给建议，由有法定责任的人终判，Agent 权限写死「只读 + 草稿」。
- **AI 用在哪？** → 治擎=导航型；星核=人在回路的分析型编排；PromptLab=产品本身。
